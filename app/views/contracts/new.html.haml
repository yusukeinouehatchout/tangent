= render 'shared/main-header', title: '契約書アップロード'

.main-container__contents
  = form_with model: @contract do |f|
    .main-container__contents__group
      = f.label :pdf_data
      %div
        = f.file_field :pdf
    .main-container__contents__group
      = f.label :pass, 'パスワード'
      %div
        = f.password_field :pass
    .main-container__contents__group
      = f.submit "send"
